version: "3.8"

services:
  postgres:
    image: postgres:16-alpine
    container_name: shiftapp_postgres
    environment:
      POSTGRES_DB: shiftapp_dev
      POSTGRES_USER: shiftapp_user
      POSTGRES_PASSWORD: shiftapp_pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U shiftapp_user"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
